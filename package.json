{
  "name": "enhanced-confluence-search-results-extension",
  "private": true,
  "version": "2.0.8",
  "scripts": {
    "version:sync": "node scripts/sync-version.mjs",
    "version:check": "node scripts/sync-version.mjs --check",
    "release:patch": "npm version patch --no-git-tag-version && npm run version:sync",
    "release:minor": "npm version minor --no-git-tag-version && npm run version:sync",
    "release:major": "npm version major --no-git-tag-version && npm run version:sync",
    "test:ui": "npm --prefix ui run test",
    "test:ui:coverage": "npm --prefix ui run test:coverage",
    "lint": "npx --yes eslint@8.57.0 . --ext .js,.jsx,.mjs",
    "build:ui": "npm --prefix ui run build",
    "build:dist": "npm run version:sync && node scripts/build-dist.mjs all",
    "build:dist:chrome": "npm run version:sync && node scripts/build-dist.mjs chrome",
    "build:dist:firefox": "npm run version:sync && node scripts/build-dist.mjs firefox",
    "build:store-images:chrome": "node scripts/build-store-images.mjs",
    "zip:dist": "node scripts/zip-dist.mjs all",
    "zip:dist:chrome": "node scripts/zip-dist.mjs chrome",
    "zip:dist:firefox": "node scripts/zip-dist.mjs firefox",
    "verify:dist": "node scripts/verify-dist.mjs",
    "build:dist:checked": "npm run version:check && npm run build:dist && npm run verify:dist",
    "smoke:confluence": "node .codex/skills/confluence-ui-smoke/scripts/run-confluence-smoke.mjs",
    "smoke:confluence:setup": "node .codex/skills/confluence-ui-smoke/scripts/run-confluence-smoke.mjs --setup",
    "smoke:confluence:full": "node .codex/skills/confluence-ui-smoke/scripts/run-confluence-smoke.mjs --full"
  }
}
